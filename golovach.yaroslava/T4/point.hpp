#ifndef POINT_HPP
#define POINT_HPP

namespace golovach {
  struct Point {
    double x;
    double y;
  };
}

#endif

//структура для хранения координат
//используется везде для передачи положения центра и возврата координат
